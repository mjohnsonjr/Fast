package com.example.fast;

/**
 * Performs actions based on the selected mode the user has chosen
 * 
 * Some initial modes would be activity tracking (steps, etc) and GPS workout tracking (running or biking).
 * @author michael
 *
 */
public class ModeManager {

	//Set mode in preferences?
	
	/*
	 * Callback for mode update.
	 * (May not be necessary).
	 */
	public void update(){
		
	}
	
}
